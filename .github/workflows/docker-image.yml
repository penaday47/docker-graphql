name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Git_Semantic_Version
      uses: PaulHatch/semantic-version@v4.0.3
      with:
       major_pattern: "major:"
       minor_pattern: "feat:"
       format: "${major}.${minor}.${patch}-prerelease${increment}"
      id: version
      
    - name: Docker login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER}}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD}}
        NEW_VERSION: ${{ steps.version.outputs.version }}
      run: |
        echo "New Version: $NEW_VERSION!!!!!!!!!"
        echo "Iniciando login"
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        echo "Finalizando login"
    # - name: Build Docker Image
    #   run: |
    #     docker build -t penaday/graphql-actions:0.0.1 . 
    #     docker build -t penaday/graphql-actions:latest . 
    # - name: Push Docker Image
    #   run: |
    #     docker push penaday/graphql-actions:0.0.1 
    #     docker push penaday/graphql-actions:latest 

    

        
    # - name: Build the Docker image
    
      # run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
